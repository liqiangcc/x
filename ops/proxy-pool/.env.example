# Copy to ops/proxy-pool/.env and replace the value before starting the pool.
PROXY_POOL_API_KEY=replace-with-a-long-random-value
PROXY_POOL_PORT=5555

# Conservative defaults for public proxy sources.
CYCLE_GETTER=300
CYCLE_TESTER=120
GET_TIMEOUT=8
TEST_TIMEOUT=5
TEST_BATCH=20
